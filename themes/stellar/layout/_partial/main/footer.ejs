<%
const author = '[@' + config.author + '](' + config.url + config.root + ')';
const using = '[' + stellar_info('name') + '](' + stellar_info('tree') + ')';
const source = theme.footer.source;
function layoutDiv() {
  var wrapper = '<footer class="page-footer fs12">[![jsDelivr](https://badgen.net/badge/:subject/:status/:color?icon=github)](https://www.jsdelivr.com)</footer>';
  return wrapper;
}
%>
<%- layoutDiv() %>

